由於此資料為多元類別資料，我們選擇使用RandomForeset來當作我們此次的預測模型。在處理資料前，我們預先將所有三筆訓練資料合併，欲找出其中有用的變數。
我們先將身高體重幾個大約1/10的缺失值利用平均數0做替代，而在BEHAVIOR的極少數缺失值中我們利用眾數去做替代，並把擁有過多缺失值的幾個變數直接做刪除。在放入所有變數做預測時，我們發現該預測準確度僅有不到五成的正確率，明顯得知在眾多變數中，有許多變數是我們用不到的，或是在這些未處理過的變數中，或許有些能用的變數未善加處理過。
我們計算出各個變數對BUY_TYPE的重要程度，前幾名的重要變數由預算為影響最多，再來則是年齡、身高體重、性別，在這幾個變數中，透過將訓練資料拆成3/4來做預測，我們得出最高的預測準確率是0.9，是由預算、年齡、性別作為主要變數來預測BUY_TYPE。
接著我們開始準備做變數處理，想辦法提高預測準確率，由於在計算重要程度時，身高體重佔比很高，但帶入預測模型中卻降低預測準確率，所以我們打算利用身高體重換算成BMI，由於變數解釋上的身高體重為標準化，我們利用台灣男女個別平均身高體重及標準差將資料轉成原始身高，當代入計算好的BMI時，也是降低我們預測模型的準確率。
重要程度上的幾個變數嘗試過後，我們開始嘗試使用直覺上比較有用的變數，例如小孩個數，或許小孩個數不是那麼重要，而是有無小孩與否，所以我們把小孩個數的變數改成，沒有小孩為0，以及有小孩為1這樣的虛擬變數，來帶入RF預測模型，而跑過之後仍會降低準確率，目前還沒有比由預算、年齡、性別組成還高的準確率。
